# 2026-09-08

- 数据源重大决策：放弃 Steam，改为纯 PSNProfiles 单一数据源（target_games.json 已全部 PSN 匹配、steam_appid 均为 null）。
- 重写 README.md 与 docs/plan.md：PSNProfiles 四类数据（奖杯元数据 / 稀有度 / 指南数据 / 完成度）分别映射到 Q2（IRT 难度 b 代理）、Q5（特征+SHAP）、Q1（玩家分群）。
- 关键口径：稀有度 = 获得人数/拥有者（相对百分比），获得人数、拥有者、百分比三者必须同时入库；稀有度随抓取批次漂移，建模需绑定数据版本。
- 遗留：AGENTS.md 仍含 Steam 规范（steam_api.py/steamspy.py/xiaoheihe.py 模块、"禁止混合 Steam/PSN 稀有度口径"条目），待同步更新。
- 注意：该项目无独立 git 仓库，git 根在上层个人目录（且有 index.lock 权限问题），不要在上层仓库 commit。
- 追加：plan.md 第六节加入成就名称映射方案（Steam 接口 name=内部名称、displayName=显示名，以 name 为键 JOIN 生成标准成就数据文件）。此方案与"纯 PSNProfiles"定位并存——用户仍会处理部分 Steam 接口数据。
- 追加（22:38）：数据源最终定为纯 Steam API 生态（GetOwnedGames / GetPlayerAchievements / GetGlobalAchievementPercentagesForApp / appdetails / SteamSpy），弃用 PSNProfiles。全局完成率 percent 作 IRT 难度 b 代理；成就名称映射（name→displayName JOIN）为主线核心清洗步骤，映射需输出差集报告。README 与 plan.md 已按 Steam 口径全量重写。
- 追加（22:53）：研究范围最终收敛为 Q2（IRT）+ Q5（特征回归+SHAP）双主线，Q1/Q3/Q4 裁剪。README 与 plan.md 已重写；AGENTS.md（建模规范、modeling/survival+cluster 目录）与主线不符，待同步。
- 追加（23:03）：plan.md 与 learning-logs/2026-09-08.md 删除后从零重写为干净版本。目标定稿为"分析用户和玩家更愿意玩什么样的游戏"，研究范围 Q1（IRT 支撑）+ Q2（偏好核心），数据源纯 Steam API。
- 工具坑：同一文件并行多个 Edit 会竞态覆盖，必须串行编辑 + grep 验证。
