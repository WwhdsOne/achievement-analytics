# 2026-09-08

## 今天做了什么

- **目标定稿**：总目标为"分析用户和玩家更愿意玩什么样的游戏"，"愿意玩"操作化为参与度指标（游玩时长 / 成就完成深度 / 活跃度 / 全成就比例）。
- **研究范围收敛**：只保留 Q1（IRT 难度建模，支撑）+ Q2（偏好回归 + SHAP 归因，核心），分群 / 弃坑生存分析 / 跨游戏迁移全部裁剪。Q1 产出难度结构特征喂给 Q2。
- **数据源定稿**：纯 Steam API 生态（GetOwnedGames / GetPlayerAchievements / GetGlobalAchievementPercentagesForApp / appdetails / SteamSpy），弃用 PSNProfiles。
- 重写 README.md、docs/plan.md、AGENTS.md 三个文档：
  - 成就名称映射（`name`=内部名 → `displayName`=显示名 JOIN）升为主线核心清洗步骤，要求输出差集报告；
  - 全局完成率 percent 作 IRT 难度 b 代理指标，三种用法对比（外部代理 / 贝叶斯先验 / 对照特征）；
  - AGENTS.md 删 PSN 混合口径条目，新增名称映射硬性要求与全局完成率版本绑定；建模规范改为回归 R²/RMSE + SHAP + IRT 收敛诊断。
- 删除旧 plan.md 与本日志，从零重写为干净版本（当前文件）。

## 遇到什么坑

- **工具坑**：同一文件并行发多个 Edit 会产生竞态，部分编辑被覆盖（编号错乱、内容丢失），必须改为串行逐个编辑并 grep 验证。适用于所有文档编辑任务。
- **过程坑**：一天内数据源反转两次（PSN → Steam）、研究范围从 5 个问题裁到 2 个，文档反复重写。下次改口径前先在群里跟队友确认清楚，避免返工。

## 明天计划

- 清理 modeling/ 下过时的 cluster、survival 子包（与主线不符）。
- 实现 crawler/steam_api.py 骨架：GetGlobalAchievementPercentagesForApp + appdetails 批量查询 + 缓存 + 限速。
