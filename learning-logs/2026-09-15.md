# 2026-09-15

## 今天做了什么

- **爬虫冒烟测试跑通**（黑魂重制版 570940 / 黑魂2原罪学者 335300 / 黑魂3 374320 / 只狼 814380 / 艾尔登法环 1245620）：
  - `crawler/steam_api.py` 骨架完成：限速 ≥1s、重试 ≤3、缓存层 `data/raw/cache/`、断点续爬（二次运行 0 次网络请求验证通过）
  - 全局完成率接口（免 key）5 款全部成功，成就数 41/38/43/34/42，难度分布合理（最易 ~94%，最难 10%）
  - appdetails（免 key）5 款全部成功，**成就总数与全局完成率的成就数逐一相等**——两个数据源交叉验证通过
- **两个关键实测发现（已回写 README / plan.md / AGENTS.md）**：
  1. appdetails **批量请求失效**：`?appids=a,b` 返回 400（原始逗号 URL 也一样，与编码无关），只能单 appid 请求。文档里"批量省配额"的假设作废，省配额改靠缓存层
  2. appdetails 的成就字段只有 `total` + 10 个 `highlighted` **展示名**（如 The Dark Soul），不含内部名（如 ACH41）。完整 name→displayName 映射的唯一来源是 `GetSchemaForGame`（需 API key）——**key 是名称映射 pipeline 的硬依赖**
- 补齐防护：`achievement-analytics/.gitignore`（忽略 .env / data/ / __pycache__）+ `.env.example`；`config.py` 报错信息原本引用的 `.env.example` 此前并不存在
- 新增 `docs/plan-simple.md`（一页大白话版计划）与 `crawler/steam_api.py`（含 `fetch_appdetails` / `fetch_global_achievement_percentages` / `fetch_achievement_schema` / `fetch_owned_games` / `fetch_player_achievements` + smoke_test）
- 清理已裁剪方向的目录：删除 `modeling/cluster/`（玩家分群）、`modeling/survival/`（生存分析）、`crawler/__pycache__/`；Q1 的 `modeling/irt/` 保留
- venv 初建（uv，Python 3.13），当前仅装 httpx + python-dotenv，完整 requirements.txt 待进建模阶段再装

## 遇到什么坑

- **文档假设 vs 实测**：plan.md / AGENTS.md / README 三处都写了"appdetails 支持批量"，实测 400。教训：API 行为以冒烟测试为准，文档里的接口假设上线前必须先验证
- **git 结构注意**：git 仓库根目录是整个家目录（远程 github vault），`achievement-analytics/` 尚无文件被跟踪；已加本地 .gitignore 保护 .env 与 data/，但在家目录根 `git add .` 仍可能扫进其他敏感文件（如未忽略的 AccessKey.csv 等），提交前务必检查暂存区

## 明天计划

- 申请 Steam API key（https://steamcommunity.com/dev/apikey），写入 `.env`
- 用 key 跑 `fetch_achievement_schema`，打通 name→displayName 映射，产出第一款游戏的标准成就数据文件（api_name / display_name / percent）+ 差集报告
- 确认 requirements.txt 是否剔除已裁剪方向的依赖：`lifelines`（生存分析）、`umap-learn` / `hdbscan`（聚类）——目录已删，依赖留在清单里会误导队友
